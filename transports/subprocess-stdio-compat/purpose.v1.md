# Subprocess Stdio Compat Purpose v1

## Purpose

Define `subprocess_stdio_compat` as a legacy, debug, and migration-only
process invocation boundary.

## Allowed Uses

- temporary migration compatibility
- debug-only command invocation
- old integration bridge
- smoke checks
- local developer diagnostics
