# Tools

`api/tools/` is the documentation root for projection-support tooling.

Expected future contents include schema/OpenAPI generation helpers, projection
release tooling, and validation support that operates on API-owned surfaces.

This root does not own protocol meaning or runtime behavior.
