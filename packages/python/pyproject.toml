[project]
name = "yailabs-yai-sdk"
version = "0.1.0"
description = "YAI SDK Python package"
requires-python = ">=3.10"
readme = "README.md"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
