# Delivery Workflow

1. Audit the live tree and current git state.
2. Confirm scope and boundaries.
3. Edit only the approved surface.
4. Update `.agents` in the same delivery if repo truth changed.
5. Run truthful validation and drift checks.
6. Report `.agents` updates, validation, and unresolved items.
