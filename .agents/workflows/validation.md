# Validation Workflow

1. Start with `.agents/validation/commands.md`.
2. Run `git diff --check`.
3. Run `.agents/validation/drift-checks.md`.
4. Run repo-local validation only when it is known and relevant.
