# Edit Policy

- Inspect the tree before editing.
- Prefer the smallest truthful change set.
- Respect dirty working trees and never revert unrelated user changes.
- AGENTS.1 is limited to `.agents/**`.
- If contracts, commands, boundaries, or current state change in a future delivery, update `.agents` in the same delivery.
