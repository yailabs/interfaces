# .agents Control Plane

`.agents/` is the canonical YAI-native agent control plane for the `api` repository.

It is living instruction and coordination material. It must be updated when repository truth changes and must not present imagined API structure as if it already exists.

Future architecture must be marked `planned-not-created`, external paths must be marked `external`, and anything not verified must be marked `unknown`.
