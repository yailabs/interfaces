# Quality Gates

- `.agents` is updated when repo truth changed.
- no vendor folders exist under `.agents/`
- `git diff --check` passes
- drift checks were run
- no fake observed path/module/command/ownership claim is asserted
