# Registry Generation

Registry generation creates derived indexes or projections from canonical
registry inputs.

Canonical registry truth remains in `registry/`. Generated registry surfaces
must record the source registry files, generator identity and version, source
commit or release, and conformance profile.
