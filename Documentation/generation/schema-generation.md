# Schema Generation

Schema generation produces derived schema bundles or language-facing schema
helpers from canonical schema inputs.

Canonical schema truth remains in `schemas/` and related registry and mapping
artifacts. Generated schema outputs must record input files, input digests or
versions, generator identity, source commit or release, and conformance profile.
