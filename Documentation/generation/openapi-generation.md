# OpenAPI Generation

OpenAPI is projection.

OpenAPI artifacts describe HTTP-facing views of YAI Interfaces protocol
contracts. They do not replace the protocol registry, schemas, mappings,
envelopes, errors, or transport contracts.

OpenAPI generation must record registry, schema, mapping, generator, source
commit or release, and conformance profile provenance.
