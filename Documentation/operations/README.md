# Operations

## Purpose

Operations defines the API operation grammar, registry contract, operation-to-transport mapping, and projection model for commands/actions.

## Belongs Here

Operation grammar, operation registry, dispatch mapping, command projection, action descriptors.

## Does Not Belong Here

Client UX layout, SDK package APIs, runtime handler internals, and historical command product docs.
