# Protocol Examples

Protocol examples demonstrate operation, envelope, error, and transport
contracts directly from canonical YAI Interfaces artifacts.

Protocol examples may reference OpenAPI projections, but OpenAPI is projection
and must not replace registry, schema, mapping, envelope, error, or transport
truth.
