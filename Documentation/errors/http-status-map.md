# HTTP Status Map

HTTP status mapping is the loopback and remote HTTP projection of canonical API errors.

## Artifact Reference

- `errors/error-http-status-map.v1.json`

## Rule

HTTP status communicates transport-level outcome. The response body still carries the canonical API error shape when an operation fails.
