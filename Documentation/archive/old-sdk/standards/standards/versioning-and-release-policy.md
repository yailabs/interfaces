# Versioning And Release Policy

SDK versioning applies to package APIs, compatibility aliases, generated surfaces, and developer-facing behavior.

## Policy

- Breaking package changes require explicit versioning and migration notes.
- Compatibility aliases should include deprecation posture and canonical replacements.
- Generated surfaces must record the API artifact version they consume.
- Package release claims require validation evidence.
- SDK releases do not redefine API protocol versions.
