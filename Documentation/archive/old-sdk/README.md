# Old SDK Archive

This archive contains historical material from the old SDK repository.

The old SDK repository is tombstone/historical only. Active SDK packages,
protocol/API truth, conformance, generation, provenance, examples, and package
reference live under `interfaces`.

Active package roots:

- `packages/rust`
- `packages/python`
- `packages/typescript`
- `packages/c`
