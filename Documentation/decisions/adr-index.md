# ADR Index

Historical ADR material was inspected and archived under `archive/old-flat-root/adr/` after its active API consequences were summarized here.

## Active Decision Records

- ADR-V1 session death decision: active API consequence is that API contracts must avoid hidden session ownership and require explicit identity, principal, case, operator, and client context where needed.

## Archive Pointer

Use `archive/historical-manifest.md` for the old path, new archive path, classification, and absorption notes.
