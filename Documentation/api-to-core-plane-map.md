# API to Core Plane Map

API contracts map to runtime ownership in `../yai` while preserving separation.

- API: contract, schema, envelope, status, error, lifecycle, operation registry
- YAI core: runtime implementation and adapters
- SDK: typed client consumption layer

See `registry/api-plane-surfaces.v1.json` for per-plane owner path and status.
