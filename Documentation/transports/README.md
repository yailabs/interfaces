# Transports

## Purpose

Transports documents API transport contracts and compatibility boundaries.

## Belongs Here

Local IPC RPC, loopback HTTP, event stream, secure LAN/remote HTTPS, subprocess stdio compatibility, in-process test transport, and contract indexes.

## Does Not Belong Here

Transport implementation code and transport artifact trees outside `Documentation`.
