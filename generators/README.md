# Generators

`generators/` records generator policy and future generator entrypoints for YAI
Interfaces.

Generated outputs are not protocol source of truth. Generators must consume
canonical registry, schema, and mapping inputs and must record provenance for
any generated surface retained in this repository.

Current policy:

- `generators/provenance-policy.md`
