# Examples

`api/examples/` is the documentation root for API projection examples.

Example payloads here are API-consumer aids. They do not replace canonical
protocol fixtures in `../yai/protocols/fixtures` and do not own protocol truth.

V28.6 creates this root as a projection-oriented placeholder only.
