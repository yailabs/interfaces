# Enterprise API Standard

API repository is canonical source of operation grammar and contract schemas.

Does not own:
- runtime execution
- client behavior
- SDK implementation
- command rendering

C in `contracts/` is helper/reference material; canonical grammar remains registry + schemas + OpenAPI projection.
