# Fixtures

`api/fixtures/` contains API-side fixture mirrors and projection examples.

These fixtures may mirror canonical protocol fixtures from
`../yai/protocols/fixtures` when API conformance or projection consumers still
need local copies.

They are not the canonical owner of protocol meaning.
